# firebase/functions

Cloud Functions backend for Nexiva.

## Commands

```bash
npm install
npm run build
npm run lint
```

## Exposed Functions

- `suggestFeasibility` callable: returns feasibility score + recommendation.
- `onTaskCompleted` trigger: updates XP and completion stats on task completion.
